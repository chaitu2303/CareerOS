<USER_REQUEST>
Yes. Ippudu previous prompt + nee latest requirements + screenshots lo visible issues + screenshots lo kanipinchani entire application audit anni merge chesi, agent ki ivvadaniki oka master implementation prompt prepare chesa.

Important: Reference UI screenshot ni inspiration ga use cheyyali, exact copy kaadu. Current CareerOS identity ni preserve chesthu premium, modern, responsive product ga redesign cheyyali.

Copy this entire prompt:

# CAREEROS — COMPLETE PRODUCT RECONSTRUCTION, FUNCTIONAL COMPLETION & PREMIUM UI/UX MASTER DIRECTIVE

You are now responsible for completing CareerOS as a genuinely functional, production-grade Career Operating System.

DO NOT treat the screenshots I provided as the complete bug list.

The screenshots are only examples of visible failures. You must independently audit the ENTIRE repository, route tree, sidebar, header, dashboard cards, buttons, forms, APIs, database models, authentication flows, resume pipeline, internal links, dynamic routes, mobile layouts, empty states, and all user journeys.

Do not wait for me to manually discover every broken page.

The previous automated tests created false confidence by validating HTTP responses and limited workflows while many real user-facing features remained broken, empty, placeholder-only, incorrectly routed, or visually incomplete.

From this point onward:

- Do not claim “100% complete”
- Do not claim “production ready”
- Do not claim “Beta ready”
- Do not claim a feature works because the route returns HTTP 200
- Do not deploy repeatedly after every small fix
- Do not hide broken functionality behind placeholder pages
- Do not fabricate user data, scores, achievements, jobs, events, assessments, or activity
- Do not stop after fixing only the issues visible in screenshots

Your task is to perform a COMPLETE PRODUCT AUDIT, create a dependency-aware implementation plan, implement the fixes, validate locally, deploy once the release candidate is genuinely ready, and then run production acceptance testing.

==================================================
PHASE 0 — REPOSITORY & PRODUCT DISCOVERY
==================================================

Before modifying code, inspect the entire repository.

Build a complete inventory of:

1. App Router routes
2. Dynamic routes
3. API routes
4. Sidebar navigation items
5. Header navigation and controls
6. Dashboard cards and CTAs
7. Command palette actions
8. Profile dropdown actions
9. Notification actions
10. Settings links
11. Authentication pages
12. Onboarding steps
13. Database models
14. Resume ingestion pipeline
15. Native Intelligence engines
16. Browser Extension integration
17. All buttons, links, forms, modals and dropdowns
18. Desktop-only and mobile-only components
19. Placeholder pages
20. Hardcoded mock data
21. Dead code
22. TODO/FIXME markers
23. “Milestone X” placeholders
24. Empty feature shells
25. Routes referenced by UI but missing from the filesystem

Create a canonical PRODUCT CAPABILITY MATRIX.

For every visible feature record:

- Feature name
- Route
- UI entry point
- API dependencies
- Database dependencies
- Current status
- Broken dependencies
- Required implementation
- Acceptance criteria

Do not begin with cosmetic redesign before understanding the architecture.

==================================================
PHASE 1 — FIX NAVIGATION AND ROUTING COMPLETELY
==================================================

Create one canonical route registry used by:

- Sidebar
- Mobile navigation
- Dashboard CTAs
- Header
- Search / command palette
- Breadcrumbs
- Profile menu
- Notifications
- Internal redirects

Every visible navigation item must point to a real route.

Known examples of production failures include:

- Career Passport → 404
- ATS Analysis → 404
- Settings → 404
- Application Copilot incorrectly resolving as an application ID
- Other potentially broken routes not shown in screenshots

These are examples only.

Audit ALL routes.

For every route:

- direct browser navigation must work
- client-side navigation must work
- refresh must work
- authenticated access must work
- unauthenticated access must redirect correctly
- no accidental dynamic-route collision
- no 404
- no blank page
- no unhandled exception

Fix static-vs-dynamic route architecture.

For example:

/dashboard/applications/copilot

must be a genuine standalone route and must never be interpreted as:

/dashboard/applications/[id]

Dynamic pages must only receive genuine database IDs.

Create a route-crawler test that:

1. authenticates
2. collects every internal href
3. visits every route
4. verifies expected page rendering
5. fails on 404
6. fails on 500
7. fails on blank content
8. records console errors
9. records failed network requests

==================================================
PHASE 2 — AUTHENTICATION MUST BE COMPLETE
==================================================

Validate the complete authentication system:

- Email/password registration
- Email/password login
- Google OAuth
- Show/hide password
- Forgot password
- Password reset
- Logout
- Session persistence
- Protected route access
- New user routing
- Existing user routing
- Cross-user data isolation

Required journeys:

New email user:
Register → onboarding → dashboard

New Google user:
Continue with Google → onboarding → dashboard

Existing completed user:
Login → dashboard

Logged-out user:
Protected route → login

Password reset:
Forgot Password → secure token → reset → login with new password

Never allow redirect loops between onboarding and dashboard.

==================================================
PHASE 3 — REBUILD RESUME INGESTION AS A VERIFIED TRANSACTION
==================================================

The current resume upload/extraction flow has failed in production.

Do not consider this fixed until a REAL PDF and REAL DOCX complete the entire workflow.

Required pipeline:

Upload
→ validate file
→ securely store/process
→ extract text
→ validate extracted text
→ structured parsing
→ display verification UI
→ allow user corrections
→ user confirms
→ persist to Prisma/Neon
→ reload data
→ render Master Profile
→ recalculate profile completion
→ propagate profile data across CareerOS

Support:

- PDF
- DOCX

Handle:

- malformed files
- password-protected files
- image-only/scanned PDFs
- empty extraction
- unsupported formats
- duplicate uploads
- extraction timeout
- parser failure

Never return “success” with empty extracted content.

If OCR is not available, clearly tell the user that a scanned/image-only PDF requires OCR or manual profile creation.

Extract where available:

- Full name
- Email
- Phone
- Location
- Professional summary
- Skills
- Work experience
- Education
- Projects
- Certifications
- Achievements
- Portfolio
- GitHub
- LinkedIn
- Other relevant links

The extraction result must be editable before final confirmation.

Use the existing Native Intelligence architecture honestly.

Do not claim external AI is being used if it is not.

Replace inaccurate copy such as:

“We’ll use AI to extract…”

with truthful product language such as:

“CareerOS will analyze your resume and prepare a structured profile for your review.”

Do not expose implementation details unnecessarily to users.

==================================================
PHASE 4 — MASTER PROFILE AS THE SINGLE SOURCE OF TRUTH
==================================================

Master Profile must become the canonical career data source.

Implement complete CRUD for:

- Personal details
- Professional summary
- Skills
- Experience
- Education
- Projects
- Certifications
- Achievements
- Links
- Career preferences
- Target roles
- Target locations

Every update must:

- save to database
- survive refresh
- be isolated per user
- update profile completion
- propagate to dependent modules

Dependent modules include:

- Dashboard
- Career Passport
- Job Intelligence
- Resume Studio
- ATS Analysis
- Assessments
- Coding Practice
- Career Paths
- Simulations
- Interviews
- Application Copilot
- Application Tracker
- Performance
- Career Coach

Fix any state where onboarding is completed but profile completion incorrectly remains 0%.

==================================================
PHASE 5 — COMPLETE EVERY PRODUCT MODULE
==================================================

Do not leave sidebar items as empty pages.

The final information architecture may be simplified and reorganized, but every visible item must have a genuine purpose.

Recommended primary navigation:

- Dashboard
- Onboarding / Profile Setup
- Jobs / Job Intelligence
- Assessments
- Coding Practice
- Career Paths
- Simulations
- Applications
- Performance
- AI Career Coach
- Tools
- Settings

Existing advanced modules can be grouped under these sections instead of creating an excessively long sidebar.

For example:

JOBS
- Job Intelligence
- Job Targets
- Recommendations

APPLICATIONS
- Application Copilot
- Application Tracker

PROFILE
- Master Profile
- Career Memory
- Career Passport

PRACTICE
- Assessments
- Coding Arena
- Department Practice

SIMULATIONS
- Mock Interviews
- Recruitment Simulator

TOOLS
- Resume Studio
- ATS Analysis
- Other utilities

Do not remove valuable existing features.
Reorganize them into a clearer product architecture.

--------------------------------------------------
DASHBOARD
--------------------------------------------------

Build a real personalized command center.

Display only data backed by the actual user account.

Possible sections:

- Greeting
- Profile completion
- Skills strength
- Active applications
- Interview performance
- Recommended next actions
- Recommended jobs or targets
- Career journey
- Skill visualization
- Recent activity
- Upcoming tasks
- Achievements
- Career Coach entry point

IMPORTANT:

The provided premium dashboard screenshot is a DESIGN DIRECTION reference.

Use its strengths:

- clean premium layout
- warm neutral surfaces
- strong visual hierarchy
- organized cards
- rounded but professional components
- useful dashboard density
- elegant sidebar
- readable typography
- strong spacing
- subtle illustration
- clear information architecture

DO NOT blindly copy fake content from the screenshot.

Do not display:

- fake 78% profile completion
- fake 24 applications
- fake interview scores
- fake TCS/Zoho/Swiggy recommendations
- fake events
- fake recent activity

unless these records genuinely exist.

For a new account, show meaningful empty states and onboarding actions.

--------------------------------------------------
ASSESSMENTS
--------------------------------------------------

The page must not permanently show:

“No assessments available.”

Provide a functional starter assessment system.

Include:

- categories
- difficulty
- questions
- attempts
- answers
- scoring
- result review
- progress persistence

Use seeded, curated or deterministic content.

Do not generate fake completion records.

--------------------------------------------------
CODING PRACTICE
--------------------------------------------------

Do not leave:

“No problems published yet.”

Create a functional starter problem library.

Include:

- problem list
- difficulty
- topics
- problem details
- examples
- test cases
- attempt state
- submission history
- progress

Do not pretend arbitrary code execution exists if a secure execution environment is unavailable.

Clearly support only what the architecture genuinely provides.

--------------------------------------------------
CAREER PATHS / DEPARTMENT PRACTICE
--------------------------------------------------

Remove “Milestone 9” placeholder UI.

Build real role/domain learning tracks.

Examples:

- Frontend Development
- Backend Development
- Full Stack
- Java Development
- Data Science
- Cybersecurity
- Cloud
- DevOps

Each path should include:

- skill map
- current readiness
- missing skills
- recommended learning sequence
- practice actions
- progress

--------------------------------------------------
MOCK INTERVIEWS
--------------------------------------------------

Remove “Milestone 10”.

Build an actual interview workflow.

Include:

- role selection
- interview type
- question session
- answer input
- feedback
- session summary
- history

Use the existing Native Intelligence architecture where appropriate.

Do not pretend external AI services are active.

--------------------------------------------------
RECRUITMENT SIMULATOR
--------------------------------------------------

Do not display fabricated scores.

Every score must come from an actual attempt.

Support a real multi-stage hiring simulation with available capabilities.

Unavailable capabilities must be clearly represented without fake success.

--------------------------------------------------
APPLICATION COPILOT
--------------------------------------------------

Build a real standalone Copilot workspace.

It must not show “Application not found” because of route collision.

Support:

- selected job context
- match analysis
- profile compatibility
- missing skills
- application preparation
- extension handoff
- save to tracker

--------------------------------------------------
APPLICATION TRACKER
--------------------------------------------------

Implement genuine CRUD:

- create
- view
- edit
- status change
- delete
- notes
- company
- role
- job URL
- dates
- stage

All data must persist and remain isolated per user.

--------------------------------------------------
CAREER PASSPORT
--------------------------------------------------

Build the missing page.

Display genuine:

- verified skills
- completed assessments
- achievements
- certifications
- evidence
- career readiness

Do not fabricate verification.

--------------------------------------------------
ATS ANALYSIS
--------------------------------------------------

Build or correctly map the missing route.

Support the real workflow:

Resume
+ optional target job description
→ analysis
→ score breakdown
→ keyword/skill gaps
→ actionable recommendations

Scores must be deterministic and explainable.

--------------------------------------------------
PERFORMANCE
--------------------------------------------------

Use actual activity data.

Show:

- assessment progress
- coding progress
- application progress
- interview progress
- profile progress

No fabricated analytics.

--------------------------------------------------
AI CAREER COACH / NATIVE CAREER COACH
--------------------------------------------------

The product must remain independent of mandatory paid third-party AI APIs.

Use the existing Native Intelligence architecture.

The coach must be honest about its capabilities.

It may provide:

- contextual recommendations
- next best actions
- profile improvement suggestions
- job preparation guidance
- navigation to relevant CareerOS modules

Do not present deterministic heuristics as a third-party LLM.

--------------------------------------------------
SETTINGS
--------------------------------------------------

Create the missing Settings page.

Include working controls for:

- Profile/account
- Appearance
- Theme
- Preferences
- Security
- Password management where applicable
- Connected authentication methods
- Data/privacy controls where supported

==================================================
PHASE 6 — COMPLETE PREMIUM UI/UX RECONSTRUCTION
==================================================

The current UI should be upgraded substantially.

Use the attached premium dashboard reference as visual inspiration, but maintain an original CareerOS identity.

DESIGN GOALS:

- premium
- modern
- elegant
- highly readable
- professional
- warm and human
- suitable for students and professionals
- visually rich without being cluttered

Create a unified design system.

Define tokens for:

- background
- surface
- elevated surface
- primary text
- secondary text
- muted text
- border
- primary brand
- success
- warning
- error
- shadows
- radii
- spacing
- typography

CRITICAL CONTRAST RULE:

Never allow text to visually merge with its background.

Never use:

- dark text on dark buttons
- white text on very light backgrounds
- low-contrast placeholder text
- transparent overlays that make labels unreadable
- hover states that destroy contrast
- disabled states indistinguishable from enabled states

All buttons must have explicit states:

- default
- hover
- focus
- active
- disabled
- loading

All form controls must have:

- visible labels
- accessible associations
- clear focus states
- error states
- success states
- readable placeholder text

==================================================
PHASE 7 — FULL LIGHT/DARK THEME SYSTEM
==================================================

Implement a real Light/Dark theme toggle.

The toggle must:

- exist in the header
- also be configurable in Settings
- persist across refresh
- respect system preference on first visit
- avoid hydration mismatch
- update the entire application consistently

Support:

- Light
- Dark
- System

Do not simply invert colors.

Create separate semantic theme tokens.

Validate contrast in both themes.

Every component must be checked in:

- light mode
- dark mode
- hover state
- focused state
- disabled state
- loading state
- error state

Charts, icons, borders, cards, inputs, modals, dropdowns, tooltips and navigation must all adapt correctly.

==================================================
PHASE 8 — RESPONSIVE DESIGN FOR ALL DEVICES
==================================================

CareerOS must be mobile-first and fully responsive.

Test at minimum:

320px
360px
375px
390px
412px
430px
768px
820px
1024px
1280px
1440px
1920px

Mobile requirements:

- sidebar becomes accessible drawer
- hamburger/menu button
- correct overlay behavior
- body scroll lock when drawer opens
- close on navigation
- proper touch targets
- no horizontal overflow
- no clipped cards
- no overlapping text
- no fixed desktop widths
- responsive forms
- responsive tables
- responsive charts
- responsive modals
- readable typography
- sticky controls only when useful

Tablet must have its own intentional layout.

Do not merely shrink the desktop UI.

==================================================
PHASE 9 — COMPLETE INTERACTION AUDIT
==================================================

Audit EVERY:

- button
- link
- icon button
- menu
- dropdown
- tab
- card CTA
- form submit
- upload control
- delete action
- edit action
- save action
- cancel action
- search action
- filter
- pagination control
- notification
- theme toggle
- profile menu
- sidebar item

For every interaction verify:

1. it is clickable
2. it performs the intended action
3. it provides loading feedback when necessary
4. it handles success
5. it handles failure
6. it does not silently fail
7. it does not create console errors
8. it works with keyboard navigation
9. it works on mobile

No decorative button should appear interactive unless it actually works.

==================================================
PHASE 10 — EMPTY, LOADING AND ERROR STATES
==================================================

Every data-driven page must have:

- loading state
- empty state
- error state
- retry action where appropriate
- populated state

Avoid dead messages such as:

“No data.”
“Not available.”
“Check back later.”

Instead provide useful next actions.

Example:

Instead of:
“No assessments available.”

Use:
“You haven’t started an assessment yet.”
[Browse Assessments]

But only if the Browse Assessments destination genuinely works.

==================================================
PHASE 11 — DATABASE AND DATA INTEGRITY
==================================================

Audit Prisma and Neon integration.

Verify:

- migrations/schema alignment
- required indexes
- user ownership
- cascading behavior
- duplicate prevention
- transaction safety
- nullable fields
- correct relation loading

Every user-owned record must be scoped by authenticated user ID.

Explicitly test cross-user isolation.

User A must never access User B’s:

- profile
- resume
- applications
- assessment attempts
- coding history
- interview sessions
- career data

==================================================
PHASE 12 — BROWSER EXTENSION INTEGRATION
==================================================

Do not ignore the CareerOS extension.

Validate:

- extension authentication handoff
- profile retrieval
- safe autofill
- sensitive field blocking
- job extraction
- match analysis
- Save to Tracker
- application handoff

If extension authentication cannot be fully automated, preserve an explicit manual smoke-test checklist.

Do not mark the extension fully validated without an actual browser smoke test.

==================================================
PHASE 13 — REAL ACCEPTANCE TESTING
==================================================

Replace shallow route testing with user-journey testing.

HTTP 200 does not equal PASS.

A feature passes only if the expected user outcome occurs.

Test two account types:

A. Completely new user
B. Returning populated user

Required new-user journey:

Register
→ onboarding
→ upload real PDF
→ extraction
→ verification
→ save profile
→ dashboard
→ profile data visible
→ refresh
→ data still visible

Repeat with DOCX.

Required returning-user journey:

Login
→ dashboard
→ edit Master Profile
→ save
→ refresh
→ verify persistence
→ run Job Intelligence
→ create resume version
→ run ATS analysis
→ start assessment
→ complete attempt
→ verify score
→ create application
→ update application
→ verify performance data
→ logout

Test Google OAuth separately.

Test mobile journeys separately.

==================================================
PHASE 14 — VISUAL REGRESSION AND QUALITY
==================================================

Capture screenshots for major pages at:

- desktop light
- desktop dark
- mobile light
- mobile dark

Inspect for:

- overlapping text
- clipped text
- low contrast
- excessive whitespace
- inconsistent spacing
- broken cards
- overflow
- misaligned icons
- unreadable buttons
- theme errors

The current resume upload error screen is an example of unacceptable UX:

- generic error
- poor visual feedback
- unclear recovery path

Replace generic errors with useful, user-friendly error states.

==================================================
PHASE 15 — DEPLOYMENT STRATEGY
==================================================

Do not deploy after every small fix.

Workflow:

1. Audit
2. Create implementation plan
3. Fix architecture
4. Fix core data flows
5. Complete modules
6. Redesign UI/UX
7. Add responsive behavior
8. Run local typecheck
9. Run production build
10. Run database validation
11. Run local acceptance tests
12. Fix failures
13. Repeat until clean
14. Deploy one release candidate
15. Run the complete production acceptance matrix

Required technical checks:

- npx tsc --noEmit
- production build
- Prisma validation
- route crawl
- authenticated E2E
- real PDF upload
- real DOCX upload
- database persistence
- cross-user isolation
- mobile viewport tests
- dark/light theme tests
- console error audit
- network failure audit

==================================================
FINAL RELEASE GATE
==================================================

Produce a final matrix containing EVERY major feature:

Feature
Route
Desktop
Mobile
Light Theme
Dark Theme
Functional Test
Persistence Test
Status

Allowed statuses:

PASS
INTENTIONALLY UNAVAILABLE
FAIL

No P0 failures may remain.

No P1 failures may remain for core user journeys.

Do not hide failures.

Do not convert a failure into “provider blocked” unless an actual optional external provider is genuinely required.

CareerOS core functionality must not depend on mandatory paid third-party AI APIs.

FINAL RULE:

Do not stop when the screenshots I provided look fixed.

The screenshots are only evidence that the previous validation process was incomplete.

Continue until the ENTIRE application has been audited and every visible user-facing feature has either:

1. been fully implemented and genuinely tested, or
2. been intentionally removed from the UI because it is not ready.

A visible feature must never lead to:
- 404
- blank page
- placeholder milestone
- fake data
- broken button
- silent failure
- generic unexplained error

Build CareerOS as one coherent product, not a collection of disconnected milestone demos.

Na suggestion: agent ni direct ga “implement everything now” ani start cheyinchadam kanna, first repository-wide audit + capability matrix + dependency-aware implementation plan generate cheyinchi, auto-proceed cheyyamani cheppu. Main priority order: routes/auth → resume extraction → Master Profile/database → core modules → UI redesign → responsive/theme → full acceptance tests → single production deployment.

Nee first reference image direction current UI kanna chaala better structured ga undi. Kani screenshot lo unna fake numbers/data ni copy cheyyakunda, actual database data + strong empty states tho same premium quality target cheyyali.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-07-12T17:50:10+05:30.

The user's current state is as follows:
Active Document: e:\Ai Job Tracer\careeros\src\app\(auth)\reset-password\page.tsx (LANGUAGE_TSX)
Cursor is on line: 1
Other open documents:
- e:\Ai Job Tracer\careeros\src\app\(auth)\reset-password\page.tsx (LANGUAGE_TSX)
- e:\Ai Job Tracer\careeros\package.json (LANGUAGE_JSON)
No browser pages are currently open.
</ADDITIONAL_METADATA>